<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>あっち向いてホイ</title>
</head>
<body>
    <p>あなたは<%= your %>を向きました．</p>
    <p>コンピュータは<%= cpu %>を指しています．</p>
    <p>判定：<%= judgement %></p>
    <p>現在<%= total %>試合中<%= win %>勝しています．</p>
    <hr>
    <form action="/attimuitehoi">
        <input type="text" name="face" required>
        <label for="face">次は何を出す？</label>
        <input type="hidden" name="win" value="<%= win %>">
        <input type="hidden" name="total" value="<%= total %>">
        <input type="submit" values="あっち向いて　ホイ">
    </form>   
</body>
</html>